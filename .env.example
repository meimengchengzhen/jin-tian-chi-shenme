# 「今天吃什么」环境变量样例
# 复制为 .env 并填入你的值；.env 已在 .gitignore，请不要提交。

# ===== Supabase 云端同步（可选）=====
# 不填默认走纯本地模式，所有功能仍然可用，仅缺少跨设备同步。
# 配置后会启用「云端同步 Beta」入口。
# 详细搭建步骤见 docs/supabase.md。

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# ⚠️ 安全：
# - 仅使用 anon public key；service_role 严禁出现在前端。
# - 启用 RLS（migration.sql 已包含）确保用户只能读写自己的行。
